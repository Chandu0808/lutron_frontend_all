/** Customized variant settings layout adapter — Phase 5.2 */

export const customizedSettingsLayoutAdapter = {
  variant: "customized",
  /** Customized uses SettingsSidebar component instead of SharedSettingsShell grid. */
  useStandaloneSidebar: true,
};

export default customizedSettingsLayoutAdapter;
